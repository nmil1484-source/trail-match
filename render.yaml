services:
  - type: web
    name: trail-match
    runtime: node
    region: oregon
    plan: free
    branch: main
    buildCommand: pnpm install && rm -rf dist && pnpm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
